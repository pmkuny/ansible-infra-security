- name: Create admin group
  group:
    name: "{{ base.admin_group }}"
    state: present

- name: Install issue file
  template:
   dest: /etc/issue
   src: templates/issue
   owner: root
   group: support
   mode: 0664
